root: ">>> os.path.dirname(self['__dir__'])"
tags: ["demo"]
api:
    host: "!!!"
    port: 80
    db:
        driver: mysql
        user: "!!!"
        password: "!!!"
        name: demo_db
    secret: "!!!"
    logging: error
frontend:
    host: "!!!"
    port: 80
    js:
        merge: yes
        minify: yes
    css:
        merge: yes
        minify: yes
    templates:
        reload: no
        cache: yes
    logging: error
    assets: ">>> os.path.join(self['root'], 'frontend', 'assets')"
